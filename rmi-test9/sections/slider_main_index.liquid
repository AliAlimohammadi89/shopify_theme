 

   
 <div class="main-slider-all" style="overflow: hidden;">
    <div class="gallery flickity-mainSlider js-flickity-main" style="height: 100%">
 {% for block in section.blocks %}  
        <img class="mySlides" src="{{ block.settings.images | img_url: '2048x' }}" alt="{{ block.settings.images.alt }}" style="width:100%;height: 100%">
    {% endfor %}     
  </div>
  </div>

  

{% schema %}
 {
  "name": "Images Slider",
	"max_blocks": 20,
  "settings": [
    {
      "id": "image_slider",
      "type": "text",
      "label": "Heading",
      "default": "Title"
    }
  ],
  "blocks": [
    {
      "type": "select",
      "name": "Add Button",
      "settings": [
       {
        "id": "images",
        "type": "image_picker",
        "label": "images"      
}
        
      ]
    }
  ],
  "presets": [
    {
      "name": "slider home page",
      "category": "index",
      "blocks": [
        { 
          "type": "select"
        },
        {
          "type": "select"
        }
      ]
    }
  ]
}

{% endschema %}
 


 

{% stylesheet %}
 
 
{% endstylesheet %}

 
